import { defineConfig } from 'vite';

export default defineConfig({
  // Por defecto, Vite usa 'public' como publicDir.
  // Si necesitas personalizar la raíz (root) o la carpeta pública (publicDir), puedes hacerlo aquí:
  // root: './', // Normalmente no es necesario cambiar esto
  // publicDir: 'public', // Este es el valor por defecto
});
